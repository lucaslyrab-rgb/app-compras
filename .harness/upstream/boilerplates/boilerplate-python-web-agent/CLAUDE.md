# CLAUDE.md

As instrucoes do projeto ficam no `AGENTS.md`, compartilhado com Codex e outras ferramentas:

@AGENTS.md

## Claude Code

- Rode `make test` antes de dar uma tarefa como concluida.
- Para ver a aplicacao funcionando, use `make dev` e acesse `http://localhost:8000`.
- Ao mudar comandos, arquitetura ou convencoes, atualize o `AGENTS.md` (nao este arquivo).
