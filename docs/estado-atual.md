# Estado atual

- **Objetivo:** substituir WhatsApp/planilhas por uma PWA multiusuário para o fluxo FLV das três lojas.
- **Mudança ativa:** `openspec/changes/fundacao-plataforma/` (fundação técnica e primeiro fluxo vertical).
- **Revisão:** branch `main`; commit inicial já sincronizado com `lucaslyrab-rgb/app-compras`; materiais originais preservados na raiz.
- **Decisões válidas:** [TRD](trd.md), [ADRs](adrs/), [PRDs](prds/) e [roadmap](roadmap.md).
- **Última evidência (2026-09-20):** `npm ci`, lint, typecheck, build, 17 testes unitários (cobertura de linhas 80,35%), 5 testes de integração em PostgreSQL 18.6 efêmero, importação repetida 74/21, Playwright (7 testes verdes + 1 skip desktop, acessibilidade/zoom/login inválido/fluxo móvel) e migration/importação one-shot no database compartilhado (`app_compras`: 3 lojas, 74 produtos, 21 exclusivos); imagem multi-stage não-root saudável e Trivy local sem HIGH/CRITICAL. A stack foi reconciliada com `infra/stack.yml` no Swarm como `app-compras_web` (1/1, digest `b7e5ae…`, rollback configurado), `/api/health` respondeu pelo domínio HTTPS e o ensaio isolado de backup age/restore recuperou `3/74/21`. Verificação direta em execução confirmou `SHOW server_version = 18.6 (Debian 18.6-1.pgdg13+2)` e a existência de `app_compras`; a documentação de execução agora usa o bucket `s3://compras-bkp` em `us-east-1`.
- **Implementado:** schema/migration, autenticação/RBAC, sessões revogáveis, catálogo/inativação, pedido móvel persistente/versionado, conferência A4, Docker, stack Swarm e workflows CI/release. O GitHub Actions publicou e escaneou `sha-e6a782563f526b431d036b1456842fd3da81bbcb`, promoveu-a no manifesto e o Swarm foi reconciliado manualmente; a imagem está saudável com rollback configurado. O webhook GitOps ainda aguarda cadastro/validação.
- **Credenciais iniciais:** Gestor criado por bootstrap one-shot e confirmado no banco: `lucaslyrab@hotmail.com`, papel `GESTOR`, ativo. A senha aleatória foi entregue diretamente ao operador e não está registrada no repositório, imagem ou documentação.
- **Ferramentas operacionais (2026-09-20):** host Ubuntu 24.04 agora possui AWS CLI `2.36.49`, `age` `1.1.1`, `pg_dump`/`pg_restore` `16.15` (cliente compatível com o servidor PostgreSQL `18.6`). A identidade AWS validou e o bucket `compras-bkp` respondeu com sucesso; a identidade privada `age` ainda precisa ser provisionada em cofre separado.
- **Backup:** bucket S3 `compras-bkp`, região `us-east-1`, informado pelo operador; ainda falta executar backup cifrado fora do host e restauração/rollback comprovados.
- **Pendências externas:** cadastrar Registry/Webhook no Portainer e secret `PORTAINER_WEBHOOK_URL`; executar deploy homologado, ensaio de backup/restauração/rollback e UAT dos papéis Loja/Comprador/Gestor em aparelhos reais.
- **Adoção Portainer:** migração executada com autorização. A stack `app-compras` agora é Portainer ID 4, Swarm `primary`, Git `refs/heads/main`, compose `infra/stack.yml`, Registry GHCR ID 1, polling de 5 minutos e webhook presente; o webhook foi testado com HTTP 204. Serviço `1/1` e healthcheck HTTPS permaneceram saudáveis.
- **Roteiro UAT:** [docs/operacao/uat.md](operacao/uat.md) foi preparado para registrar evidência por aparelho e papel; a aprovação real ainda não foi registrada.
- **Notificação SES:** o job `Notify release by email` foi publicado para enviar sucesso/falha via SES. Após o cadastro dos secrets AWS, o job terminou com `success` no run `35530921993`; o release geral falhou somente em `Trigger Portainer GitOps webhook`, portanto a mensagem enviada registra a falha sem promover uma versão não confirmada.
- **Próxima ação autorizada:** concluir os itens OpenSpec que exigem esses recursos operacionais, sem marcar evidência ausente como concluída; PostgreSQL compartilhado confirmado na versão 18.6.
